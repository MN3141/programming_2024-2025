This files serve the purpose of reducing the time needed to install common tools used by me.
Steps:
    -run toolchain_fetch.bat
    -run common_apps_fetch.bat
    -install the applications based on fetched files
    -install Microsoft Terminal
    -integrate Git Bash and MSYS2 into Microsoft Terminal
    -add *MSYS2_path*/ucrt64/bin to Path variable
    -use MSYS2 to fetch GNU tools
    -install Visual Studio Code extensions
